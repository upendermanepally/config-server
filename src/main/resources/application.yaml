server:
  port: 8086

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/upendermanepally/config-server.git
          default-label: master
          clone-on-start: true
          #force-pull: true

#commenting as it comes by default with eureka client dependency
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/


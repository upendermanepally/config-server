server:
  port: 8086

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/upendermanepally/config-server.git
          clone-on-start: true
fav:
  num: 7
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/

